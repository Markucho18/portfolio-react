const projects = [
  {
    name: "Pomodoro Timer",
    
  }
]

const Projects: React.FC = () => {
  return (
    <section className="w-full bg-red-500">
      <p>Projects</p>
    </section>
  )
}

export default Projects